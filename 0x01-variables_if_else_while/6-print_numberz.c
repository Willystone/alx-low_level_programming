#include <stdio.h>
/**
 * main - print if the number is postive, zero, or negative.
 * Return: Always 0
 */
int main(void)
{
char b;
for (b = '0'; b <= '10'; b++)
{
	putchar(b);
}
printf('\n');
return (0);
}
